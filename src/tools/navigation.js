export const navigateFromTile = (label, navigate) => {
    switch(label.toLowerCase()) {
        case "myCompanies":
            break;
        case "customers":
            break;
        case "invoices":
            break;
        case "products":
            break;
        case "taxes":
            break;
        default: 
            break;
    }
}