import { StyleSheet } from 'react-native';

import { 
    hp, 
    wp, 
} from '../../tools';

export default StyleSheet.create({
    flatListContainer: {
        width: '100%', 
        paddingHorizontal: wp(16), 
        paddingTop: hp(24),
    },
});