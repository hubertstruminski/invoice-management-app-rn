import React from 'react';
import { Text } from 'react-native';

import { BasicView } from '../../components';

const LoginScreen = () => {
    return (
        <BasicView>
            <Text>The LoginScreen component</Text>
        </BasicView>
    );
}

export default LoginScreen;