import { StyleSheet } from 'react-native';

import { hp } from '../../../core/tools';

export default StyleSheet.create({
    appTitleContainer: {
        marginBottom: hp(48),
    },
    container: {
        alignItems: 'center',
        paddingTop: hp(48),
    },
});